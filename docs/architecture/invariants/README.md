# Invariants

Business rules that MUST NEVER be violated.

## Format

Each invariant defines:
- **Rule:** What must always be true
- **Enforcement:** How it's enforced in code
- **Violations:** What counts as a violation
