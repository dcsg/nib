# Toast
<!-- nib-component: v1 | generated: 2026-03-03 | do not edit manually -->

> A transient message that confirms an action or communicates status — auto-dismisses after a timeout

**Widget type:** `toast`

---

## Anatomy

| Part | Description |
|------|-------------|
| `root` | The toast container (role=status or role=alert) |
| `icon` | Optional semantic icon matching the variant |
| `title` | Optional short heading |
| `message` | The primary message text |
| `action` | Optional inline action (e.g. Undo, View) |
| `dismissButton` | Optional manual dismiss control |

---

## States

| State | Description |
|-------|-------------|
| `entering` | Toast is animating in |
| `visible` | Toast is fully visible; timer is running |
| `dismissing` | Toast is animating out |
| `dismissed` | Toast has been removed from the DOM |

---

## Variants

- `info` — Neutral information — role=status (polite live region)
- `success` — Positive outcome — role=status (polite live region)
- `warning` — Caution required — role=alert (assertive live region)
- `error` — Failure or blocking issue — role=alert (assertive live region)
## Accessibility

**ARIA role:** `status`
**Focus behavior:** not-focusable
**Focus trap:** No
**Focus returns to:** previously-focused-element  
**Required label:** No (message-text-is-accessible-via-live-region)
**Touch target minimum:** 44pt iOS / 48dp Android / 24px web

### Keyboard Interaction

| Key | Behavior |
|-----|----------|
| `Escape` | Dismisses the toast immediately |
| `Tab` | Moves focus to the action or dismiss button within the toast |

### ARIA Attributes

- `aria-live`
- `aria-atomic`
- `aria-label`

---

## Token Bindings

```json
{
  "root": {
    "visible": {
      "background": "color.surface.elevated",
      "color": "color.text.primary",
      "border-radius": "border-radius.md",
      "shadow": {
        "offsetX": "0px",
        "offsetY": "4px",
        "blur": "16px",
        "spread": "0px",
        "color": "rgba(0,0,0,0.1)"
      }
    }
  }
}
```

---

*Generated by nib. Run `nib brand build` to regenerate.*
